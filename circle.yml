dependencies:
  pre:
    - pip install coverage

test:
  override:
    - nosetests --with-coverage --cover-html --cover-html-dir=$CIRCLE_ARTIFACTS
        parallel: true
        files:
          - tests/**/*.py
